export const RELEASE_NOTES = {
    version: "v2.9.1",
    notes: `v2.9.1:
- added: "sort by tag" and "add to sort" context menu commands
- added: "text" standard sort option
- added: store sort, grouping, and collapse state in per-query options

v2.9.0:
- added: 'no grouping' mode for paragraph-level cross-note sorting
- added: search / extend query from editor context menu
- added: cmd/ctrl+click to extend query from nav panel
- added: saved queries: limit option, column rename syntax
- added: settings: hide tag prefix, auto-open panel, panel visibility
- improved: parallelise note content fetching in batches
- improved: numeric-aware comparison in tag range filtering
`};